# Loose lower bounds on purpose: this has to install on whatever Python the Mac
# happens to have, and that is not worth debugging at 3am.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pyserial>=3.5
numpy>=1.26
opencv-python>=4.10
httpx>=0.28
python-dotenv>=1.0
websockets>=13.0
# The native /responses path in robot/brain.py. Optional in spirit -- CORGI_ROUTER_BACKEND
# defaults to "keyword" and make_router() degrades cleanly if this is missing -- but
# small and cross-platform enough to just install by default rather than by hand.
merge-gateway-python>=0.3

# Tests. `pytest` runs the whole suite against the simulator, no hardware, no network.
pytest>=8.0
pytest-asyncio>=0.24

# Hardware only, installed by hand on the machine that needs them.
#   feetech-servo-sdk   the SO-101 arm bus. Only needed with CORGI_ARM_ENABLED=1.
#   pupil-apriltags     real tag detection for the return leg. No wheels on very new
#                       Pythons, and robot/tags.py falls back to color without it.
