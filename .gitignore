__pycache__/
*.py[cod]
.Python
.venv/
venv/
.env
.DS_Store
.idea/
.vscode/
*.ino.cpp
.arduino/
node_modules/
.cache/
*.egg-info/
dist/
build/

# The Photon/Spectrum bridge is its own scaffolded project with its own git repo --
# `bun create spectrum-project@latest corgi --projectId ... --providers imessage`.
# It is not part of this repo's history, the same way .venv/ is not.
/corgi/

# Local camera probe dumps (may contain faces / unrelated frames)
_cam_probe/
camera_check.jpg

# Scratch recording runs — keep only the training set we intentionally commit
datasets/walker_pill_grasp_test/
